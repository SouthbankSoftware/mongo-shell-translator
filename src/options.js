export const parseOptions = {
  tolerant: true,
  raw: true,
  tokens: true,
  range: true,
  comment: true,
};
export const syntaxType = {
  callback: 'callback',
  promise: 'promise',
  await: 'await',
};
